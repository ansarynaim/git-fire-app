//creating a context of user and its resources so that we can access all the details easily without props drilling 

import { createContext } from "react";

export const UserContext = createContext();
